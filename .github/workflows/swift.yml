name: Merge or commit

on:
  push:
  pull_request:

jobs:
  build:

    runs-on: macos-15

    steps:
    - uses: actions/checkout@v7
    - name: Build
      run: swift build -v
    - name: Run tests
      run: swift test -v
